export default function Root(props) {
  return (
    <section
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        marginTop: "10rem",
        fontSize: "3rem",
      }}
    >
      This is the Dogs microfrontend
    </section>
  );
}
